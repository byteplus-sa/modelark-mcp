## Summary

- **AGENTS.md update**: Expand the Project section to list all 10 product surfaces (Seed Audio, Seedream, Seedance, Seed 3D, Seed 2.1 Understanding, Speech-to-Text, VOD AI MediaKit, Object storage upload, Durable artifacts, Transports). Fix provider count from "two" to "three" (ModelArk, Seed Speech, VOD AI MediaKit).
- **SKILL.md update**: Bring the modelark-mcp skill fully in sync with the latest MCP server code:
  - Add Seed 3D bullet to intro and frontmatter description
  - Add complete Seed 3D tool reference (8 tools, full parameter tables, examples)
  - Update model capability registry from 9 to 11 families (add Hyper3D, Hitem3d)
  - Fix architecture section (5→4 API surfaces, ModelArk row lists all 4 products)
  - Update health resource, artifact expiry, and retry guidance for 3D
  - Add Seed 3D Async Workflow usage pattern
  - Add 3D common issues and best practices (items 19-20)
  - Expand Environment Essentials with 3D, rate limiting, readiness, JWT discovery, persistence cache, state backend, and seed understanding vars

## Test plan

- [ ] Verify AGENTS.md renders correctly and the product list matches the server's `register_tools` function
- [ ] Verify SKILL.md tool reference sections match the actual tool implementations in `src/modelark_mcp/tools/`
- [ ] Verify the model capability registry table matches `src/modelark_mcp/config/model_capabilities.py`
- [ ] Verify the environment variables listed in SKILL.md match `.env.example` and `src/modelark_mcp/config/env.py`
- [ ] Verify the `.claude/skills/modelark-mcp` symlink resolves to the same content as `.agents/skills/modelark-mcp/SKILL.md`
